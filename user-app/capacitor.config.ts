import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tunjabus.passenger',
  appName: 'TunjaBusPassenger',
  webDir: 'out'
};

export default config;
